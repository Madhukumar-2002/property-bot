server.port=${PORT:8080}

# Active profile
spring.profiles.active=dev

# WhatsApp API Configuration
# Set these as environment variables in Railway dashboard
whatsapp.token=${WHATSAPP_TOKEN}
whatsapp.phoneId=${WHATSAPP_PHONE_ID}
